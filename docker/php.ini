; CPQ 开发环境 PHP 基础配置（容器与本地共用基线）
; 运行时差异（debug 等）由 .env 控制，不在此处硬编码
memory_limit = 512M
upload_max_filesize = 20M
post_max_size = 20M
max_execution_time = 120
date.timezone = Asia/Shanghai
display_errors = On
; mpdf / PhpSpreadsheet 临时目录指向可写路径
sys_temp_dir = /tmp
